class Solution:
    def plusOne(self, digits: List[int]) -> List[int]:
        sum=0
        for i in digits:
            sum=(sum*10)+i
        sum=sum+1
        arr=[]
        while(sum>0):
            arr.append(sum%10)
            sum//=10
        return arr[::-1]
