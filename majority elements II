class Solution:
    def majorityElement(self, nums: List[int]) -> List[int]:
        d=Counter (nums)
        n=len(nums)
        a=[]
        for key , value in d.items ():
            if(value>n//3):
                a.append(key)
        return a

        
        
